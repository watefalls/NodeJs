<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor"
      crossorigin="anonymous"
    />
    <title><%= title %></title>
  </head>
  <body>
    <% if(created) {%>
    <div class="alert alert-success">Note has been created</div>
    <% } %>

    <div class="container">
      <h1>This is working</h1>

      <form action="/" method="post" class="mb-2">
        <div class="from-group mb-2">
          <input type="text" class="form-control" name="title" required />

          <button class="btn btn-primary mt-2" type="submit">Send</button>
        </div>
      </form>

      <% if(notes.length) {%>

      <div class="card">
        <ul class="list-group">
          <% for(let i = 0; i < notes.length; i++) { %>
          <li
            class="list-group-item d-flex justify-content-between align-items-center position-relative"
          >
            <p><%= notes[i].title %></p>
            <div class="rule__items d-flex">
              <button
                data-type="change"
                class="btn btn-primary me-2"
                data-id="<%= notes[i].id %>"
              >
                Редактировать
              </button>

              <button
                class="btn btn-danger"
                data-type="remove"
                data-id="<%= notes[i].id %>"
              >
                &times;
              </button>
            </div>
            <div
              class="updated__block position-absolute top-0 start-0 bottom-0 end-0 d-flex justify-content-between align-items-center p-2 bg-light"
              style="z-index: -1"
              data-type="update"
            >
              <input
                type="text"
                class="form-control"
                value="<%= notes[i].title  %>"
                style="width: 100px"
                data-id="<%= notes[i].id  %>"
              />
              <div class="buttons">
                <button
                  class="btn btn-success"
                  data-type="update"
                  type="submit"
                >
                  Сохранить
                </button>
                <button class="btn btn-danger" data-type="cancel">
                  Отменить
                </button>
              </div>
            </div>
          </li>
          <% }%>
        </ul>
      </div>

      <%} else {%>
      <p>No notes</p>
      <%}%>
    </div>
    <script src="./app.js"></script>
  </body>
</html>
